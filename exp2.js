const mathutil={
    basic:{
        add: (a, b) => a + b,
        sub: (a, b) =>  a - b,
        multiply:(a, b) => a * b,
        divide: (a, b) => a / b,
    },
    advanced: {
        square: (a) => a * a,
    }
};
module.exports = mathu;